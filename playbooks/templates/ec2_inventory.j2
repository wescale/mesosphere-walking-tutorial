[upper_mesosphere]
{% for host in groups['upper_mesosphere'] %}
{{ host }} ansible_ssh_private_key_file={{ ssh_private_key }} zoo_keeper_index={{ loop.index }}
{% endfor %}

[lower_mesosphere]
{% for host in groups['lower_mesosphere'] %}
{{ host }} ansible_ssh_private_key_file={{ ssh_private_key }}
{% endfor %}